#usda 1.0
# Modified by the openusd Ruby project only to normalize the final newline.
(
    "Test to verify that usdcat can isolate just the layer metadata."
    customLayerData = {
        string key = "value"
    }
    endTimeCode = 100
    startTimeCode = 1
    subLayers = [
        @sublayer.usd@
    ]
)
def "Prim"
{
}
